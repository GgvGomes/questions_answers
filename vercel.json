{
    "version": 2,
    "builds": [
        {
            "src": "./api/server.ts",
            "use": "@vercel/noce"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "./api/server.ts"
        }
    ]
}